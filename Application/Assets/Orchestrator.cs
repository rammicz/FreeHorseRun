using UnityEngine;
using System.Collections;
using System.Linq;

public class Orchestrator : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //var components = GetComponents<GameObject>();
        //var finish = components.FirstOrDefault(c => c.name == "Finish");
        //var finishColider = finish.GetComponentsInChildren<BoxCollider>();

        //var player = components.First(c =>c.name == "Player");



        //finishColider.
	}

    void FixedUpdate()
    {

    }

	// Update is called once per frame
	void Update () {

    }
}
